// Pawn
const RADIUS = 10
const MOVEMENT_SPEED = 3

// Game
const GAME_SPEED_RATE = 16.67

// Network
const INTERPOLATION_OFFSET = 200 // Should be close to BROADCAST_RATE_INTERVAL on server for better performance

export { RADIUS, MOVEMENT_SPEED, GAME_SPEED_RATE, INTERPOLATION_OFFSET }

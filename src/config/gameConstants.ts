// Pawn
const RADIUS = 10
const MOVEMENT_SPEED = 2

// Game
const GAME_SPEED_RATE = 16.67

// Network
const INTERPOLATION_OFFSET = 500 // Should be equal to frequency of websocket messages from server

export { RADIUS, MOVEMENT_SPEED, GAME_SPEED_RATE, INTERPOLATION_OFFSET }
